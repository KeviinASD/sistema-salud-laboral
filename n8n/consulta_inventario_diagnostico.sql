-- CONSULTA DE DIAGNÓSTICO: Verificar si hay datos en la tabla
-- Ejecuta esta primero para verificar que hay datos

SELECT COUNT(*) as total_items FROM inventario;

-- Si retorna 0, no hay items en la tabla
-- Si retorna un número mayor a 0, entonces hay items pero ninguno cumple la condición


